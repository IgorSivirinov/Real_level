<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blueMain</item>
        <item name="colorPrimaryDark">@color/blueMain</item>
        <item name="colorAccent">@color/blueMain</item>
    </style>
    <style name="ProgressBar_Home" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progessbar_home_xp</item>
        <item name="android:minWidth">200dp</item>
    </style>

    <style name="MyToolbarTheme"
        parent="@style/ThemeOverlay.AppCompat.Dark">
        <item name="colorControlNormal">#FFFFFF</item>
        <item name="titleTextColor">#FFFFFF</item>
    </style>
</resources>
